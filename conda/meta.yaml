package:
  name: hdf5able
  version: VERSION_NUMBER

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - numpy
    - h5py
    - pathlib # [not py3k]
    - enum    # [not py3k]
	
test:
  imports:
    - hdf5able

about:
  home: https://github.com/menpo/hdf5able/
  license: BSD License
